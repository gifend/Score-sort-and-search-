# Score-sort-and-search-
用于存放成绩显示、排序、查找程序的源代码
其中MyFrame.java为使用swing编写的具体框架，包括标签排布，按钮功能，颜色等。
ScoreEcxeption.java中ScoreEcxeption为自定义的异常类，可当导入的数据不正确会将该异常抛出。
Student.java定义了student这个类，用于记录该学生的id和成绩等信息。
StudentList.java定义了StudentList这个类，该类用于记录了所有学生的信息，并定义了按成绩排序、按学号查找的函数。
testProg.java中定义了main函数。
